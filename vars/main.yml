---

# bihealth.nginx ----------------------------------------------------------------------------------

nginx_reverse_proxy_port: 8080

nginx_static_pages:
  - location: /
    name: index
    html_content: |
      <h1>REST API Only</h1>
